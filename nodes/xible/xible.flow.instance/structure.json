{
  "name": "xible.flow.instance",
  "type": "object",
  "description": "The instance of the flow which is currently running.",
  "outputs": {
    "instance": {
      "type": "xible.flow.instance"
    },
    "state": {
      "type": "math.number",
      "description": "Returns the running state of the flow instance. 0=stopped, 1=stopping, 2=initializing, 3=initialized, 4=starting, 5=started."
    },
    "timing": {
      "type": "object",
      "description": "Returns the timing object on the flow instance."
    }
  }
}

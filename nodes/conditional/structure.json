{
  "name": "conditional",
  "type": "object",
  "description": "Return the selected value(s) based on the condition.",
  "inputs": {
    "condition": {
      "type": "boolean"
    },
    "if true": {
      "description": "If the condition is truthy, this input is passed to the 'value' output.",
      "assignsOutputType": "value",
      "assignsInputType": "if false"
    },
    "if false": {
      "description": "If the condition is falsy, this input is passed to the 'value' output.",
      "assignsOutputType": "value",
      "assignsInputType": "if true"
    }
  },
  "outputs": {
    "value": {}
  }
}
